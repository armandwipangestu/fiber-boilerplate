package {{ .Pkg }}

import "time"

// Create{{ .ID }}Request is the payload for creating a {{ .Pkg }}.
type Create{{ .ID }}Request struct {
	Name string `json:"name" validate:"required,min=2,max=120"`
}

// Update{{ .ID }}Request uses pointers so omitted fields are not overwritten.
type Update{{ .ID }}Request struct {
	Name *string `json:"name" validate:"omitempty,min=2,max=120"`
}

// ListQuery holds the pagination query params.
type ListQuery struct {
	Page    int `query:"page"`
	PerPage int `query:"per_page"`
}

// PaginationMeta mirrors the repository's pagination metadata.
type PaginationMeta struct {
	Page       int   `json:"page"`
	PerPage    int   `json:"per_page"`
	TotalItems int64 `json:"total_items"`
	TotalPages int   `json:"total_pages"`
}

// {{ .ID }}Response is the public representation of a {{ .Pkg }}.
type {{ .ID }}Response struct {
	ID        string    `json:"id"`
	Name      string    `json:"name"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}

// List{{ .Plural }}Response wraps a page of {{ .Pkg }}s.
type List{{ .Plural }}Response struct {
	Items []{{ .ID }}Response `json:"items"`
	Meta  PaginationMeta      `json:"meta"`
}