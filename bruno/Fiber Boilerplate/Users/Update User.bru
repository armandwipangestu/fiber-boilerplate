meta {
  name: Update User
  type: http
  seq: 4
}

patch {
  url: {{host}}/api/v1/users/{{user_id}}
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "name": "Jane Updated"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}