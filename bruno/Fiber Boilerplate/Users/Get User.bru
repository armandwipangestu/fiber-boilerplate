meta {
  name: Get User
  type: http
  seq: 2
}

get {
  url: {{host}}/api/v1/users/{{user_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}