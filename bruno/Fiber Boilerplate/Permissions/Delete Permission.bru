meta {
  name: Delete Permission
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/v1/permissions/{{permission_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}